## Структура монорепозитория с несколькими сервисами в отдельных контейнерах

payment_services/  
├── auth/                     # Микросервис авторизации (JWT, refresh/ access токены и пр.)   
├── users/                    # Микросервис управления пользователями (логины, контакты, аудит и т.д.)  
├── webhook_2can/             # Микросервис обработки вебхуков от провайдера 2can  
├── .env                      # Файл с переменными окружения для локальной разработки  
├── .env.example              # Пример файла .env для новых разработчиков  
├── .gitignore                # Правила игнорирования файлов в Git  
├── docker-compose.yml        # Файл оркестрации Docker: запускает все микросервисы и зависимости (БД, Redis и т.п.)  
└── README.md                 # Описание структуры верхнего уровня: назначение репозитория, список сервисов, инструкция по запуску  

## 📝 Описание структуры:  
auth/ — микросервис, отвечающий за аутентификацию и выдачу JWT-токенов (access и refresh), а также за управление сессиями и безопасностью.  
users/ — микросервис для управления пользователями: создание уникальных логинов, хранение контактной информации (включая second_login), аудит действий и взаимодействие с БД (mydb, paydb).  
webhook_2can/ — микросервис, принимающий и обрабатывающий вебхуки от платёжного провайдера 2can; включает валидацию, надёжную доставку и интеграцию с другими сервисами через RabbitMQ.  
.env, .env.example — файлы конфигурации окружения; используются для настройки сервисов в Docker и локальной разработке.  
.gitignore — исключает временные, лог- и секретные файлы из контроля версий.  
docker-compose.yml — централизованный файл для запуска всей системы: поднимает все микросервисы, PostgreSQL, Redis, RabbitMQ и другие зависимости.  
README.md — общий документ верхнего уровня с описанием архитектуры, инструкцией по первому запуску и ссылками на документацию отдельных сервисов. 

## Запуск всех сервисов, в том числе БД:
### через общий docker-compose.yml:
```commandline
services:
  postgres:
    image: postgres:16
    container_name: dev-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 15

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: dev-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - backend

  webhook:
    build: webhook_2can
    container_name: dev-webhook
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      RABBITMQ_URL: amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASS}@rabbitmq:5672/

    depends_on:
      - postgres
      - rabbitmq
    networks:
      - backend

volumes:
  postgres_data:

networks:
  backend:
    driver: bridge

```
